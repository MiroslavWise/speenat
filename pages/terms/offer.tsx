import { NextPage } from "next";

const Offer: NextPage = () => {

        return (
                <>
                </>
        )
}

export default Offer

export const getServerSideProps = () => {
        return {
                props: {
                        
                }
        }
}